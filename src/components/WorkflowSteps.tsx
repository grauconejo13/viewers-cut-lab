const steps = ["Viewers choose", "Integrity checks validate ballots", "Gemini interprets the audience signal", "Creator reviews and approves", "Screenplay scene is generated", "Continuity and story logic are checked"];
export function WorkflowSteps() { return <section className="section workflow" id="how-it-works" aria-labelledby="workflow-title"><div className="section-heading"><p className="eyebrow">The lab workflow</p><h2 id="workflow-title">Human judgment stays in the frame.</h2></div><ol>{steps.map((step, index) => <li key={step}><span>{String(index + 1).padStart(2, "0")}</span><p>{step}</p></li>)}</ol></section>; }
